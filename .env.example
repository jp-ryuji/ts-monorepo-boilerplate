# External port mappings
API_PORT_EXTERNAL=3000
WEB_PORT_EXTERNAL=3001
DB_PORT_EXTERNAL=5432
REDIS_PORT_EXTERNAL=6379

# Database configuration
DB_USER=saas_user
DB_PASSWORD=saas_password
DB_NAME=saas_db
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}

# Redis configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Environment
NODE_ENV=development

# Web configuration
NEXT_PUBLIC_API_URL=http://api.saas.local:3000

# Optional admin tools (uncomment to enable)
# PGADMIN_PORT_EXTERNAL=5050
# PGADMIN_EMAIL=admin@saas.local
# PGADMIN_PASSWORD=admin
# REDIS_COMMANDER_PORT_EXTERNAL=8081
